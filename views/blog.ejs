<%- include("partials/header")-%>

<div class="body">
	<h1>Blog</h1>
	<% posts.map((post, index) => { %>
	<h3><%= post.title %></h3>
	<p>
		<%= post.body.substring(0,100) + "..." %>
		<a href="posts/<%=index%>">Read More</a>
	</p>
	<% }) %>
</div>
